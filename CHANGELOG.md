# 更新日志

## v3.5.0 — 修掉一批"安静判错"的问题 (2026-09-15)

v3.4.0 把行业从 2 个扩到 9 个、加了自定义词库、重做了手机端。这一版回头
把**上一版没验到的地方**补上 —— 全部问题都有一个共同特征：**不报错、不崩溃、
界面看起来正常，但结论是错的**。

### 🔴 蓝V / 非蓝V 分档完全失效（判定口径，P0）

`guardian/engine.py` 里有一行：

```python
def _account(self) -> str:
    # 简化：引擎内部用 non_blue_v 取默认严重度（调用方已通过 filter 控制）
    return "non_blue_v"
```

**引擎把账号类型写死成"非蓝V"**，完全忽略用户在界面上选的蓝V。后果是
`rules/blue_v_only.json` 那 57 条双档规则（保健食品 / 医疗器械 / 处方药 /
法律咨询…）的分档全部失效：

| 文案 | 用户选择 | 修复前 | 修复后 |
|---|---|---|---|
| 「关于保健食品的合规说明」 | 蓝V | violation | warning |
| 「关于保健食品的合规说明」 | 非蓝V | violation | violation |

Web 端的 `severityOf(rule, accountType)` **一直是对的** —— 同一段文案、
同一个选项，两端给出不同等级。现在账号类型从 `detect_text` 一路传到
`_mk_hit`，不再有全局写死的档位。

顺带修掉一个更隐蔽的：`data/config.json` 里 `last_account_type` 存的是
`"personal"`，而分档键只有 `blue_v` / `non_blue_v` —— 非法值会被**静默**回退
到非蓝V档。现在入口处统一走 `normalize_account_type()` 归一。

### 📉 「误报率 0%」这个数字，之前有 7 个行业包没被评过

README 把"误报率 35.5% → 0%"当核心卖点，但评测口径是这样的：

* `tests/corpus` 的 33 条语料**清一色是移民 / 留学话术**；
* `tests/test_false_positive.py` 调 `detect(text, ...)` 不传 `industries`，
  于是走 `data/config.json` 里写死的 `enabled_industry_packs = ["immigration"]`。

结论：**v3.4.0 新增的 7 个包、313 条规则，一条都没进过误报评测**。
"误报率 0%"对新行业不成立，只是没人量过。

现在：

* 语料按行业分档（`INDUSTRY_CLEAN` / `INDUSTRY_RISKY`），33/33 → **63/69**；
* 评测口径改为**磁盘上全部行业包**（最严口径，与 Web 端默认一致）；
* 补三条"防静默退化"守卫：
  - 每个行业包必须有正反语料（否则新包会无验证地进来）；
  - 行业包必须**真的在承重** —— 全关时漏检率必须变差，不成立说明规则被
    搬进通用库、行业包成了空壳；
  - 已修的误报逐句钉住，防止后继改动放回来。

### 🩹 实测修掉 9 处真误报（都是"把合规文案判成违规"）

| 文案 | 误报原因 | 处理 |
|---|---|---|
| 光子嫩肤 / 注射美容 / 割双眼皮 / 线雕 | 项目名，合法性取决于**机构资质**，规则自己的 note 都写着"非医疗机构不得开展"，实现却是无条件 high | 降为提示级 |
| 本店承诺绝不刷单 | 否定语境 | 加语境豁免 |
| 本品不具有疾病预防、治疗功能 | 《食品安全法》第七十三条**要求写**的法定声明 | 加语境豁免 |
| 妆字号产品，不具备医疗作用 | 同上 | 加语境豁免 |
| 效果因人而异，我们不承诺疗效 | 否定语境 | 加语境豁免 |
| 本品为非保健食品 / 净值型理财产品 | 中性行业术语被当违禁词 | 降级 |

一个合规工具把法定声明判成"critical 违规"，用户会立刻不信它 —— 这类
误报的代价比漏检更高。

### 🖥️ 桌面端：行业包默认全开 + 补上启用入口

`_DEFAULT_INDUSTRY_PACKS` / `DEFAULT_CONFIG` / `data/config.json` **三处写死**
`["immigration"]`，而 Web 端默认全开 —— 双端默认口径不一致；更糟的是设置页
**没有任何地方**能开关行业包，新增的 7 个包桌面用户既看不到也开不了。

* 默认改为 `None`（= 磁盘上全部行业包）。不写死具体 id 的理由：写过一次就会
  漂 —— 加第 10 个包时没人记得回来改，而且**静默少一个行业、不报错**；
* 设置页新增「行业词库」卡片：9 个勾选框 + 全选 / 全不选 + "已启用 N / M"；
* 空列表语义 = "我明确一个行业都不要"（尊重用户的显式选择，而不是当"没配置"
  处理 —— 否则点了全不选却什么都没变）。

顺手修掉「关于」卡片的三处：

* 版本号写死 `v3.0`，实际发布的是 3.4.0 —— 用户报 bug 报的版本号是错的；
* 「行业红线」取的是 `len(user_custom)` 而不是行业包总数，475 条显示成 **0**；
* 读的元信息文件名是 `meta.json`（不存在，实际叫 `pack.json`）→ 每个包都退回
  用英文目录名当显示名。

### 📐 长页面「内容存在但够不着」

`CTkFrame` 装不下内容时不会报错，也不会自动加滚动条。Tk 的 pack 会去压
**靠后**的子控件，一路压到 0~8px —— 控件在、数据在，但看不见也拖不出来。

实测：

* 设置页内容 **1567px**，窗口最小 740px → 「关于」「数据管理」两张卡片实际高度
  **0px**，在界面上等于不存在；
* 仪表盘内容 900px，默认窗口 880px → 词库概览 BarChart 的末尾几行被压到 8px。

修法：新增 `widgets.ScrollBody` 作为页面级滚动容器（dashboard / settings），
并加 `tests/test_ui_layout.py` —— **运行期实测**每个页面在最小窗口下有没有
"够不着"的内容（静态检查发现不了这类问题）。

顺带解决一个会**静默废掉门禁**的坑：同一进程里反复创建 / 销毁 Tcl 解释器
在本机不可靠（先建过 customtkinter 控件之后，再建根窗口必失败，
报错长成 `Can't find a usable init.tcl` / `invalid command name "tcl_findLibrary"`）。
撞上它的测试会**安静地 skip**，看起来是"环境缺显示"。现在统一用 conftest 的
会话级共享根窗口，只创建一次。

### 数据

* 规则 **1276 条**（970 条带条款依据），行业包 9 个共 475 条
* 测试 **313 → 332** 条；Web 冒烟 160 项；对比度 2 主题 × 5 视图共 2634 处
* 前端词库已重新导出（`scripts/export_web_rules.py`），双端零漂移

## v3.4.0 — 行业从 2 个扩到 9 个，用户能加自己的词，手机端重做 (2026-09-15)

上一版让结论可追溯。这一版解决**覆盖面**和**可用性**两个更基础的问题：
只有移民 + 留学两个行业，等于告诉其他行业的人"这不是给你用的"；
手机上标签在顶部、设置摊开占满屏，等于"能打开但用不了"。

### 🏭 行业包 2 → 9 个（211 → 475 条）

联网核实各行业监管条文后新增 7 个包，每个都带条款依据：

| 行业包 | 条数 | 主要依据 |
|---|--:|---|
| 医疗美容 | 52 | 《广告法》第十六、十七条；《医疗机构管理条例》 |
| 教育培训 | 42 | 《广告法》第二十四条 |
| 食品保健 | 39 | 《食品安全法》第七十三条；《广告法》第十七、十八条 |
| 房地产 | 37 | 《广告法》第二十六条；《房地产广告发布规定》第十八条 |
| 电商带货 | 36 | 《广告法》第二十八条；《电子商务法》第十七条 |
| 金融理财 | 35 | 《广告法》第二十五条；《证券法》 |
| 美妆护肤 | 33 | 《化妆品监督管理条例》第二十二、四十三条 |

行业选择器、来源标签、条数统计全部由 `meta.industry_packs` 驱动，
前端零硬编码 —— 再加行业包不需要改一行前端代码。

### 📝 「我的词库」：用户自己加词，加完立刻生效

通用工具的宿命是"永远缺一个词"：每家公司都有内部禁用表达（竞品名、
以前被平台警告过的词）。新增独立页面，支持单条 / 批量添加、启停、删除、搜索、
导出 / 导入 JSON / 下载空白模板。

- 数据只存 `localStorage`，不联网不上传；导出是**裸数组**，与桌面端
  `rules/user_custom.json` 同格式，可互导。
- 与内置词条同名会被挡下并说明理由 —— 放进来会让同一处命中同时挂"内置"和
  "我的词库"两个来源，用户看到同一个词被判两遍，只会以为是 bug。
  顺带写进了「已知边界」。
- 停用开关是真的从引擎索引里摘掉，不是只把那一行变灰。

### 📱 移动端重做：按结构性缺陷改，不按"好不好看"改

标签栏在 ≤720px 变成固定在底部的 4 格导航（复用同一份 DOM，靠 CSS 换位）；
检测设置折成一行摘要并把当前配置写在摘要里；词库筛选收成折叠块 + 已启用角标；
输入控件一律 16px（iOS 对 <16px 输入框会自动放大整页）。

踩到的坑：基础样式写的是 `textarea#input`（特异性 1,0,1），窄屏只写 `textarea`
（0,0,1）压不过它 —— 字号"看着改了其实没改"，且不报错。

### 🔍 新增门禁，并立刻抓出三笔旧账

`tests/test_industry_packs.py`（23 项）写完第一次跑就红了：

1. **台账里引的是已废止的规章**。移民包的 `basis` 写《因私出入境中介活动管理
   办法》—— 2018-11-10 取消资格认定后已不再作为准入依据；留学包引的
   《自费出国留学中介服务管理规定》更是 2015-11-10 就废止了。
   全部换成现行条文：移民包改用 **2026-09-15 起施行的**《国务院关于出境入境
   管理的规定》（国务院令第 841 号）第十条六条禁令 —— 正好是今天生效，
   备案制的 90 日过渡期也从今天起算。
2. **97 条没写依据**（移民 81 / 留学 16），按类别补齐到 100%。
3. **11 条撞名**（"包过""零风险""最便宜""名校保录""保分"…）：与旧库或彼此重复。
   同一处命中挂两个来源，用户说不清按哪条算分，已删除。

冒烟测试 108 → 159 项，新增「我的词库」28 项（真点按钮、真触发检测、
真下载和上传文件，而不是查 DOM 属性）与窄屏几何 11 项。
对比度审计新增「我的词库」视图（2414 → 2632 处），并因此发现浅色主题下
11px 小字只有 4.34:1 —— 差一点点，但该响就响。

### 🔧 顺手修的坑

- **law_ref 格式门禁把更精确的写法判成格式错**：原来只认到"第 X 条"，于是
  "第十条第一项""第三百一十九条之一""两法并列"全被拦。门禁把更严谨的依据
  挡在门外，这本身就是 bug，放宽了正则。
- **覆盖率门禁的作用域是手写名单**：`_LAW_BASED_SOURCES` 只列了两个行业包，
  新版 7 个包**根本不在门禁范围内**。改成按 `industry:` 前缀匹配，
  覆盖率下限从 80% 提到 98%。
- **headless 里的 `window.confirm` 会永久挂起**：不返回也不报错，静默等满
  180s 才报 `Runtime.callFunctionOn timed out`。冤枉了三次才定位到。

## v3.3.0 — 让结论可追溯，让词库不会安静地过期 (2026-09-15)

上一版解决了「改稿闭环」。这一版解决两个**不那么显眼但决定这东西能不能
长期用下去**的问题：依据说不说到条款号、规则会不会悄悄过期。

### ⚖️ 法规依据贯通到条款号

先说结论：源词库里**本来就有** `law_ref`（覆盖 482/485 条），
但这条数据在管道中途被丢掉了 —— 它在 `guardian/detector.py` 的
`_convert()` 里被写死成 `"law_ref": ""`。于是无论桌面端还是 Web 端，
用户永远只看到"来源=广告法"，看不到"违反第九条"。字段一直在，链路没接上。

这一版把整条链路打通：`schema.Finding` → `engine._mk_hit`（所有命中都经由
这一个函数构造，接一处即可）→ `_to_finding` → `detector._convert`
→ 导出脚本 → JS 引擎 → 命中卡片 / 报告 / 批量 CSV / 词库页 / 桌面端。

- 命中卡片新增「依据」引用块：`《广告法》第九条 · 禁止使用最字级极限用语`。
  做成浅底引用区而不是又一行灰字，因为它是用户要**选中、复制、拿去对话**的东西。
- 行业包补了 115 条依据。走幂等脚本 `scripts/enrich_law_refs.py`，
  映射表连同判定理由写在代码里，**不猜法条**：宁可显示"该来源暂无条款依据"，
  也不给一条错的——用户拿着错的依据去跟平台/法务对话，比没有依据更糟。
- 刻意留空的类别在脚本里写明理由（雇主担保红线 / 违法操作 / 关系暗示
  指向买卖 offer、挂靠、走后门，属经营许可与欺诈范畴，不是广告法调整对象）。
- 元数据新增 `law_coverage` / `coverage_by_source`：法条类词库 610/710 条
  有条款依据（约 86%），这个数字可以在词库页持续盯着看。

### ⏳ 词库时效台账 + 会失败的门禁

规则驱动型工具**不会坏掉，只会过期**。平台规则改了、监管口径变了，
静态词库照常打分，只是分数开始失真——功能不报错、测试不变红、页面看不出来。
这是这类工具最隐蔽也最致命的失效模式。

- `rules/review_log.json`：每个来源登记**依据 / 上次复核日 / 复核周期 /
  复核要点**。平台规则 90 天（变动最频繁），蓝V与行业包 180 天，广告法 365 天。
- `scripts/check_rule_freshness.py`：超期即退出码 1，已进 CI。
  同时检查**台账自身的腐坏**——词库里有但台账没登记的来源（新加词库忘了登记，
  这类来源永远不会被提醒复核）。
- Web 词库页顶部新增「词库健康度」：状态点 + 各来源规则数 / 标注依据数 /
  上次复核 / 周期 / 剩余天数，超期与临期的才展开复核要点。
- 台账只导**事实**，"还剩几天"由浏览器现算。把剩余天数写进产物，
  产物就会每天漂移，双端对拍门禁会天天变红。

### 🐛 一个「属性是对的、页面是坏的」bug

体检时发现：**点「词库浏览」看起来像没反应**。

`[hidden] { display: none }` 来自浏览器默认样式表，优先级只有 (0,1,0)；
上一版给标签页加的 `.layout { display: grid }` 与它**同权重**且作者样式优先，
一句话就把它盖掉。于是三个 `<main hidden>` 全部留在布局流里，
目标页被渲染在下方约 1000px 处。额外后果：单条检测页底下一直拖着整个
批量检测面板。

而当时的冒烟断言查的是 `el.hidden`（属性值）——**属性明明是对的，测试全绿**。
所以修的时候顺带把断言改成量几何，并加了通用审计：
凡带 `hidden` 的元素都不许可见。这条守的是整类 bug，不只是某个页签。

同一类错误还出现在对比度探针里：它判定"可见"用的是盒子高度，
而收起的 `<details>` 走的是 `content-visibility` —— **不绘制但保留布局盒**，
高度量得出来（实测 294px）。等于一直在量用户根本看不见的文字。
已改用 `checkVisibility()`，并把探针从"只量默认页"扩展到
**2 主题 × 4 视图**（单条 / 批量 / 词库 / 健康度展开）。

### 🧹 顺手清掉的数据质量问题

- `note` 字段曾**一物两用**：既是给用户看的说明，又是给维护者看的定级理由。
  结果"禁止使用全网最低等极限表述｜严重度校准：《广告法》第九条：..."
  原样出现在用户界面上。已清理 4 条，并在 `guardian/rules/validate.py`
  加了校验——用户可见字段混入内部台账直接报 error，防复发。
- `immigration/pack.json` 写 `rule_count: 140`，实际 179 条；`study_abroad`
  干脆没有清单文件。清单不被运行时读取，所以没人会发现它已经错了。
  已修正 + 补建，并加测试做清单与实物对账。
- `export_web_rules.py --check` 的漂移诊断此前只能输出"未能定位到具体字段"
  （它对比的是写死的 meta 键名单）。改成通用对比，并新增**字段级**漂移识别：
  同词同来源但 `law_ref`/建议改过的，以前看不见。

### 📊 数字

| 指标 | v3.2.0 | v3.3.0 |
|---|---|---|
| 测试总数 | 276 | **290** |
| 浏览器冒烟 | 92 项 | **108 项** |
| 对比度覆盖 | 1 视图 × 2 主题 | **4 视图 × 2 主题**（2284 处） |
| 标注条款依据 | 0（字段被丢） | **610 条**（法条类 86%） |
| 时效门禁 | 无 | **台账 + CI 门禁** |
| 页面可见性 bug | 3 个页签全部未真正隐藏 | **已修 + 加通用守卫** |


## v3.2.0 — 从"一把尺子"到"改稿闭环" (2026-09-15)

上一版解决了「检测得准不准」，这一版解决「**改的过程能不能闭环**」，
并把"能看清"当成产品质量的一部分去验收。

### 🔁 改前 / 改后迭代对比（新功能）

真实的内容运营不是"检测一次"，而是**检测 → 改 → 再检测 → 再改**。
只给一个分数，回答不了这一轮到底改没改干净。

- 「存为改前」把当前版本存成基线；之后每改一次，结果区就实时给出
  **改前分 → 改后分**、**已消除 N 处**、**仍未处理 M 处**，
  以及最容易被忽略的一项：**新引入 K 处**。
- 「新引入」是这块存在的理由。很多工具只告诉你"还剩几处"，
  不告诉你"你刚才那一刀又砍出个新问题"——而改文案改出新违规是常态。
- 命中按「词面 + 类别」配对，不按字符下标——改稿会整体挪位，
  用下标比对会把"同一处被改掉了"误判成"消失一处、新增一处"。
- 基线只存在内存里，刷新即失效，不落盘、不留原文。

### 📱 PWA：可装到主屏，断网也能用

- 新增 `manifest.webmanifest` + `sw.js` + 三套图标（192 / 512 / apple-touch）。
  图标由 `scripts/make_icons.py` 生成，与页面 favicon 共用同一份盾牌几何。
- 缓存策略分两路：**HTML 走 network-first**（打开时拿到最新版本）、
  **静态资源走 stale-while-revalidate**（秒开 + 后台悄悄更新）。
  刻意**不把版本号写进缓存名**——那又是一处"同一件事存两处"。
- 冒烟测试里真的开了 `setOfflineMode(true)` 再刷新，
  验证的是"拔了网线照样跑"，不是"注册成功"。

### 🎨 对比度整改：182 处 → 0 处

- 新增 `scripts/web_contrast.mjs`：用真实浏览器遍历所有可见文字，
  算 WCAG 2.1 对比度（正常文字 4.5:1 / 大字 3.0:1），输出待整改清单。
- 量出来的问题相当集中：**深色主题次要文字只有 3.8:1，浅色主题 3.46:1**；
  黄色（1.74:1）和橙色（2.26:1）在浅色底上几乎等于没写；
  蓝色主按钮上的白字只有 3.68:1。
- 修法上**拆开了"蓝"的两种用途**：`--accent`（深底上的蓝字 / 描边，要够亮）
  与 `--accent-solid`（蓝色实底按钮，白字压上去必须 ≥4.5:1）。
  同一个变量既要当按钮底、又要当深底文字，必然顾此失彼。
- 严重度色改为**两套主题各自定义**：深色优先调的橙黄直接搬到白底上会糊成一片。

### 🖼️ 分享卡片图

- `og:image` 此前指向 `/og.png`，但仓库里根本没有这个文件——
  链接被转发到微信 / 飞书 / Twitter 时是没有预览图的白板。
- 新增 `scripts/make_og.py` 生成 1200×630 卡片，规则条数从
  `web/data/rules.json` 实时读取，不会出现"图上写 1000、页面写 1013"。

### 🔧 一致性

- **版本号收敛为单一来源**：`guardian/__init__.py` 的 `__version__`。
  此前 Web 端 `meta.engine` 是手写在导出脚本里的，页面长期显示旧版本号。
- 分享文案里的"1000 条规则"改为"1000+ 条"，不再随词库增长而失真。
- 冒烟测试 73 → **92 项**；Python 测试 **276 项**全绿。

## v3.1.0 — AI 职责重定位 + Web 端可点击试用 (2026-09-14)

这一版回答一个此前含糊的问题：**这个工具到底该怎么用 AI。**

答案是把它从"审核员"改回"改写器"，并让没有 AI 的人也能完整使用它。

### 🎯 AI 职责重定位（核心变更）

- **判定不再交给模型。** 原先的「AI 深度检测」让模型输出 `risk_summary` /
  `extra_findings`——这两项本质都是**判定**。而判定要求确定、可复现、可追溯到法条：
  模型会漏检、会凭空发明违规、同一句话问两次给两个答案。现在判定 100% 由规则引擎完成。
- **模型只负责改写。** 新增 `RewriteRequest` / `RewriteResult` 契约与提示词构建器：
  输入原文 + 规则引擎的精确命中（含法条来源与可用替换词），输出改写后文案 +
  逐处改动对照 + 明确改不了的项（`unresolved`）。
- **Provider 层重构**：抽象方法收敛为唯一的 `complete(prompt)`，
  `rewrite` / `analyze` 变成基类模板方法。新增一个后端只需十几行。
  Ollama / OpenAI 兼容 / Null 三个实现共享同一套提示词与解析逻辑。
- **错误信息人话化**：401 / 429 / 连接被拒 / 超时 / 模型不存在，分别给出可据以
  行动的说明，而不是甩一个 `URLError: [WinError 10061]`。
- **解析层容错**：模型套 markdown 代码块、把 `changes` 写成字符串数组、
  干脆不返回 JSON —— 全部归一或降级，**永不抛异常打断检测流程**。

### 🌐 Web 端：从"有个页面"到"点击就能用"

- **跨平台对比矩阵**：同一段文案分别按小红书 / 抖音 / 微信跑一遍，
  输出分数、等级、命中数与**差异归因**。
  差异分两类且都必须呈现：① 该平台独有的命中项；② 同一表述在不同平台的**定级差异**
  （真实数据：「加微信」在小红书、抖音判中危，在微信只判低危）。
  只比较①会漏掉大量真实差异 —— 这是实测才发现的。
- **零配置 AI 改写（BYO-AI）**：页面没有后端、也不上传文案，所以不内置模型。
  「复制 AI 改写指令」把命中清单 + 改写约束打包成一段文本，粘到任意 AI 即可完成改写。
- **作品自证区**：四张差异卡片，数字全部由真实词库实时算出
  （246 条平台专属规则 / 170 条移民 / 33 条留学 / 57 条蓝V定级差异），不写形容词。
- 内联 SVG 图标，消除 favicon 404。

### 🖥️ 桌面端

- 「AI 深度检测」→「AI 改写」，职责反转；新增「复制改写指令」按钮作为零配置出路。
- 改写结果面板：改写后文案（可一键复制）+ 逐处改动对照 + 需人工确认项。
- AI 不可用时不再只是弹错误，而是询问是否改走"复制指令交给外部 AI"这条路。
- 平台映射表提到模块级，消除三处各写一份、改一处漏两处的隐患。

### 🧪 测试与工具

- 新增 `tests/test_rewrite.py`（31 例）：提示词边界、JSON 容错归一、Provider 契约，
  以及一条底线测试 —— **AI 挂掉后规则检测的分数与命中必须一模一样**。
- 新增 `scripts/web_smoke.mjs`：用真实浏览器跑 Web 端 32 项冒烟检查
  （JS 报错、词库加载、检测主流程、对比矩阵、AI 区、差异区、主题、移动端溢出）。
  起因是 `web/` 无构建、无编译期检查 —— DOM id 写错只会安静地不工作，读代码看不出来。
- 测试总数 **233 → 264**，全绿。

### 📄 文档

- 新增 **`docs/AI-USAGE.md`**：完整回答"AI 用在哪、为什么"，
  含判定不给 AI 的四条理由、提示词如何焊死边界、以及没有 AI 时的三层降级。
- README 同步：AI 定位、已知边界、冒烟测试用法。

---

## v3.0.0 — 内核重构 + 双端同源 + 反误杀治理 (2026-09-14)

这一版把项目从"一个能跑的桌面工具"推到了"可出街的作品"，主要围绕
**可信度**（误报/漏检）与**可维护性**（架构分层）两条线。

### 🏗️ 架构重构（v3.0-alpha）
- **目录分层**：`core/ui/` 扁平结构 → `guardian/`（内核）+ `apps/desktop/`（桌面端）+
  `web/`（浏览器端）。内核层只允许依赖标准库与纯 Python 包，禁止 import
  tkinter / fastapi，保证两端可共用同一份引擎。
- **规则银行（RuleBank）**：规则加载、变体展开、优先级仲裁、来源标注统一收口，
  替代原先散落在 detector 里的加载逻辑。
- **匹配器可插拔**：Aho-Corasick（pyahocorasick）与纯 Python 回溯实现同接口，
  装不上 C 扩展时自动降级而不是报错。
- **四级严重度**：`critical / high / medium / low`，替代原先只有
  violation / warning 的二分法（旧体系下"封号级"和"建议修改"混在一档）。
- **双端同源**：`scripts/export_web_rules.py` 把规则导出成前端 JSON，
  `scripts/verify_web_parity.py` 用同一批语料对拍 Python 与 JS 两个引擎的结果，
  防止两端判定漂移。

### 🎯 反误杀治理（v3.0 阶段A + 本次收尾）
- **词库质量治理**：清理含 `X` 占位符的"死规则"（永不命中，只把规则条数撑好看），
  收窄泛词，按类目重分级。中立语料误报率 **74% → 3%**。
- **语境守卫升级：从「枚举短语」到「语法模式」**。
  上一版只靠 `EXEMPT_PHRASES` 静态短语表，遇到"第一步"要手工加一条，
  遇到"第二步 / 第一阶段 / 第3步"就得再加三条 —— 用有限枚举对抗无限变体。
  本次补上 `EXEMPT_PATTERNS` 语法模式层：`第X + {步|阶段|章|篇|次|位…}`、
  `首个 + {工作日|季度|阶段…}`，一次覆盖整类变体。
  **实测误报率 35.5% → 0%**，同时真极限词（销量第一 / 首家 / 国家级）
  依旧全部报出。两者用同一组回归测试互为对照。
- **配套精确规则**：豁免"第X位"（位次语义）的同时，新增正则
  `(位列|排名|位居|荣登|蝉联)…第X位` 覆盖真正的排名宣称 —— 放宽与收紧成对出现，
  而不是单边放宽。
- **死规则复活**：`保证X天获批`（移民行业最高频违规表述之一）改写为等价正则，
  覆盖"保证3个月出签"；5 条 `XX` 占位符种草规则同样改写并调整严重度。
- **补齐缺失规则**：`现成雇主 / 现成岗位 / 现成担保`（雇主担保红线）、
  `最后一波 / 最后一轮 / 名额有限 / 错过再等一年 / 仅剩N个名额`（稀缺营销），
  全部标注来源与理由。
- **严重度校准**：`全网最低 / 全国最低 / 史上最低 / 百分百` 由 medium 提到 high
  （《广告法》第九条 + 《价格法》，与极限词同级法律风险，留在"低危建议"里会被用户忽略）。

### 🐛 功能修复
- **修复检测完成后历史不入库、高亮预览按钮永久不可用**：
  `_on_detection_done` 末尾引用了不存在的局部变量 `text` / `plat_key`，
  每次检测都在最后一行抛 `NameError`；异常发生在 Tk 回调里，界面上毫无提示，
  表现为"历史记录永远是空的、高亮预览永远点不动"。现在参数由调用方显式传入，
  并补了回归测试。
- **修复 LLM 错误回调失效**：`except Exception as e: after(0, lambda: f(str(e)))`
  —— `e` 在 except 块结束时会隐式 `del`，回调真正执行时名字已不存在。
  改为先把 `str(e)` 绑定到局部变量再进闭包。
- **修复跨平台对比的"假结论"**：三平台分数并列时 `min()` 返回首个 key，
  于是输出"此文案在小红书合规分数最低（78分），建议针对性修改后再发布"，
  而三个平台其实都是 78 分。改为并列时明说"三平台无差异"，并把
  "平台独有命中"作为差异分析的主要依据。

### ⚡ 性能与体验（切页卡顿治理）
- **切页不再"组件一个个画出来"**：原先 `show_page()` 先 `pack()` 上屏再
  `refresh()`，重建过程发生在页面已可见的状态下，用户看到组件一个接一个
  从黑变正常。改为先 refresh 后上屏；页面切换由 `pack_forget/pack`
  改为 `place` 重叠定位 + `lift()`，避免 Tk 对整棵控件树重算几何。
  **仪表盘切回 540ms → 166ms**。
- **违规详情行被撑到 200px 高**：`ctk.CTkFrame(row, width=3)` 只给 width 不给
  height 时，CTkFrame 的默认 200px 高度会把整行撑开 —— 一条两行的违规项占掉
  200px，5 条浪费 800px 空白，看起来像"没数据"。改用 `tk.Frame` 作为色条。
  同源问题在仪表盘条形图里又出现一次，一并修复。
- **新增布局约定守卫** `tests/test_ui_conventions.py`：自动扫描
  "只写 width 不写 height 的 CTkFrame"，防止同类问题第三次复发。
- **主题切换加遮罩**：切主题需重建页面（仪表盘 160 个控件约 1.2s），
  直接重建会看到界面逐块变色闪烁。改为先盖提示层、延后一帧再重建。
- **历史页增量渲染**：列表签名未变化时跳过重建（151 个控件的重建
  从 1.0s+ 降为 0）。容器改用原生 `tk.Frame` 减少 canvas 数量。
- **仪表盘去重**：同一份词库数据原先被画了两遍（彩色条形图 + 带明细的列表），
  十行内容其实只有五个含义。明细并入条形图，控件数 214 → 160。
- **首屏一次性出现**：构建期间先 `withdraw()`，完成后 `deiconify()`，
  不再看到标题/卡片依次"长出来"。

### 🧪 工程化
- **测试 25 → 233 个**，新增覆盖：反误报/反漏检语料评测、UI 布局约定、
  批量检测、导出、变体抗规避、托盘、回归专项。
- **新增 `tests/corpus/` 评测语料**：31 条合规文案 + 30 条违规文案，
  输出误报率 / 漏检率 / 仅提示率三个指标，把"工具好不好用"变成可回归的数字。
- **新增 `scripts/harden_rulebank.py`**：词库加固脚本（救活死规则 + 补缺失规则 +
  严重度校准），幂等、可 `--dry-run` 预览、每条改动写明理由。
- 新增 `docs/DESIGN.md`：记录关键设计决策与权衡（为什么这样做、放弃了什么）。
- README 全面重写，与 v3.0 真实架构、真实规则条数对齐。

### 🔧 其他
- 产品名去个人化：侧栏 logo 与窗口标题统一为"合规卫士"。

---

## v2.4 — 架构升级 + 工程化完善 (2026-08-23)

### 🏗️ 架构升级
- **行业词库包架构**：引入可插拔行业词库包机制，新增行业只需在 `rules/industry_packs/` 下添加目录即可自动加载
- **移民行业合规包**：将 140 条移民行业规则从 user_custom 迁移为标准行业包，含完整元数据（版本/分类/描述）
- **单例模式**：`ComplianceDetector.get_instance()` 线程安全单例，多 UI 页面共享同一实例，避免重复加载词库

### ⚡ 性能优化
- **修复 AC 自动机关键词覆盖 bug**：同一关键词出现在多个词库（如"加微信"在三平台都有）时，旧代码中后添加的会覆盖先添加的，导致平台规则丢失。改为每个关键词存储规则列表，全部匹配不丢失。
- **预编译正则实际生效**：修复 `_compile_regex_patterns()` 编译了但 `detect()` 仍用原始 `REGEX_PATTERNS` 重新编译的问题

### 🛡️ 稳定性提升
- **配置原子写入**：用 tempfile + os.replace 保证配置写入原子性，程序崩溃不会损坏配置文件
- **配置损坏降级**：配置文件损坏时自动回退到默认值，不崩溃
- **修复重复 pack**：checker.py 中 toggle_btn 被 pack 两次的问题

### 🧪 工程化
- 新增 25 个 pytest 单元测试（核心检测 + 配置管理 + 单例 + 行业包）
- 新增 LICENSE (MIT)
- 新增 .gitignore
- 新增 requirements-dev.txt
- 重写 README，明确"垂直行业合规助手"定位与差异化

---

## v2.3 — 性能优化 + Bug修复 + 分发包 (2026-07-19)

### ⚡ 性能优化
- **修复检测引擎重复扫描bug**：当AC自动机可用时，平台规则/蓝V限制/行业红线的检测本应由AC自动机一次完成，但旧代码仍走`_find_all()`逐词扫描，导致同一文案被扫描两遍产生重复匹配。新增`aho_active`标志位，AC可用时跳过sections 2-4的冗余扫描。

### 🐛 Bug修复
- 修复设置页"抖音"显示为"抌音"的typo
- 修复`DEFAULT_CONFIG`中`last_platform`默认值"xiaohongshu"与UI使用的"小红书"不一致
- 修复`DEFAULT_CONFIG`缺少`appearance_mode`字段
- 修复默认LLM模型配置为"gpt-4o-mini"（应为本地的"qwen2.5:7b"）

### ✨ UI改进
- 跨平台对比检测：每个平台分数圆环下方增加平台名称和违规/警告数量标签
- 圆环改用grid布局居中排列，视觉更整齐

### 📦 分发包
- 创建跨平台分发包（Mac + Windows）
- `run.sh` 移除硬编码的WorkBuddy Python路径，改用系统Python查找
- `run.bat` 补充pyahocorasick依赖安装
- 新增README.md 使用说明
- 版本号更新至 v2.3

## v2.2.1 — Bug修复 + 打包分发 (2026-07-07)

### 🐛 Bug修复
- 修复 `fg_color` 重复传参崩溃：app.py 和各页面 `__init__` 同时传 fg_color 导致 TypeError
- 修复 `GradientCard.place()` 兼容性问题：customtkinter 5.2.2 不允许 place() 传 height 参数

### 📦 打包分发
- 新增 `ComplianceChecker.spec` PyInstaller 打包配置
- 打包出 `合规卫士.app`（39MB，可直接发给别的 Mac 用）
- 安装 `pyahocorasick`（AC自动机检测加速30-60倍）
- 更新 `run.sh` 启动脚本自动安装 pyahocorasick
- Xcode CLT 绕过方案：用假 lipo/install_name_tool/codesign 命令

### ⚠️ 注意
- macOS 打包 .app 不需要 Xcode CLT（已用 wrapper 绕过）
- 如需正式 code signing，仍需安装 Xcode CLT

## v2.2-modern-dashboard — 现代仪表盘风重绘 + 暗色模式 + 功能增强 (2026-07-06)

### 🎨 UI 全面重绘（现代仪表盘风）

#### 设计系统升级
- 完整双主题设计系统（Light + Dark），30+ 色值规范
- 新增 GRADIENTS / SPACING / TYPOGRAPHY / CORNER_RADIUS 层级规范
- `get_colors()` 动态取色函数，支持实时主题切换
- 消除全部 ~20 处硬编码颜色，统一使用设计系统

#### 新组件库
- `GradientCard`：渐变背景统计卡片（各带不同色渐变条）
- `GlassCard`：磨砂玻璃信息卡片（LLM分析区、Ollama状态区）
- `ProgressRing`：Canvas绘制的合规分数圆环
- `BarChart`：迷你条形图（词库概览统计）
- `StatusPill`：状态胶囊标签（替代 RiskBadge）
- `SegmentedControl`：分段选择器（平台选择、标签页切换）
- `HoverTooltip`：悬停浮窗管理器（高亮词详情弹出）
- `ToastNotification`：底部弹出精致通知

#### 页面重绘
- **侧栏**：加渐变底色条 + 导航按钮加圆角 + Logo渐变色文字 + 暗色模式切换按钮
- **检测页**：GlassCard控制栏 + SegmentedControl平台选择 + 渐变检测按钮 + ProgressRing分数圆环 + 左侧色条违规行 + GlassCard LLM区
- **仪表盘**：GradientCard统计卡片 + BarChart词库图 + 渐变按钮组 + 跨平台检测入口
- **词库管理**：SegmentedControl标签页 + 自定义Treeview样式 + 渐变标题栏对话框 + 版本管理按钮
- **设置页**：GlassCard Ollama区 + 渐变条信息卡片 + 暗色模式说明

### 🌙 暗色模式
- 完整 COLORS_DARK 色值体系（霓虹蓝强调色 + 暗卡片 + 深底）
- 侧栏底部 ☀️/🌙 切换按钮
- 切换时所有页面调用 `apply_theme()` 动态重建
- 配置持久化（appearance_mode 存入 config.json）

### 🐛 Bug 修复

#### Bug1: _init_text_tags() 缺失
- checker.py 第38行调用了该方法但文件中无定义
- 新增 `_init_text_tags()` 方法，注册4种高亮标签样式

#### Bug2: 词库编辑保存错位
- rules_manager.py save_rule() 无条件保存到 user_custom.json
- 新增 `add_rule_to_category()` 方法，支持分流保存到广告法/蓝V限制/行业红线
- rules_manager.py 保存后自动调用 `reload_rules()` 重建AC自动机

#### Bug3: AC自动机只处理 dict 类型
- detect() 中 `isinstance(rule_data, dict)` 检查 + `continue` 跳过了 tuple 类型
- 平台规则、蓝V限制、用户自定义三种 tuple 类型现在正确解包和处理
- AC自动机对全词库生效（之前只对广告法生效）

#### Bug4: config.json 配置项未被读取
- 平台和账号类型默认值硬编码
- 改为从 config_manager 读取 last_platform / last_account_type
- 检测时自动保存用户偏好到配置

### ✨ 新功能

#### 悬停浮窗 + 右键菜单
- 高亮预览模式下，鼠标悬停高亮词弹出浮窗（关键词、分类、法条、建议）
- 右键菜单：添加到词库、复制选中文本、复制全部、清空输入
- 添加到词库自动跳转到词库管理页面的添加对话框

#### 跨平台对比检测
- 平台选择器增加"对比"选项（第5项）
- 调用 `detect_all_platforms()` 同时检测三个平台
- 三平台 ProgressRing 圆环并排展示
- 差异表格：平台独有违规关键词
- 智能建议：最低合规分数平台的针对性修改建议
- 仪表盘增加"跨平台对比检测"入口按钮

#### 词库版本管理
- `backup_rules()`：备份当前词库到 `rules/backup/YYYY-MM-DD_HHMM/`
- `list_backups()`：列出所有备份目录（按时间倒序）
- `restore_backup()`：从指定备份恢复词库 + 重建AC自动机
- 词库管理页面新增"💾 备份"和"📂 恢复"按钮
- 修改/删除规则前自动触发备份

### 📦 其他
- requirements.txt 新增 `pyahocorasick>=1.4.0`
- 版本号更新至 v2.2-modern-dashboard

## v2.1-performance — 性能优化 + 词库大规模扩充 (2026-07-06)

### ⚡ 性能优化（对标句易网）

#### Aho-Corasick 多模式匹配算法
- 引入 **pyahocorasick** 库，构建 AC 自动机
- 检测速度提升 **30-60倍**（500+ 关键词单次扫描完成）
- 自动回退机制：AC 不可用時自动切换普通遍历
- 词库自动构建：应用启动时自动调用 `_build_ac_automaton()`

#### 页面懒加载
- 左侧导航切换时**按需创建**页面（而非启动时全部创建）
- 启动速度提升 **60%+**（尤其词库管理页面含大量 Treeview 组件）
- 实现 `_show_page()` 懒加载逻辑，首次切换时创建页面

#### 词库管理虚拟列表
- 使用 **ttk.Treeview** 替代 CTkScrollableFrame
- 只渲染可见行，解决 500+ 条词库时的卡顿问题
- 新增 `_build_treeview_tab()` 方法，统一构建树形表格

#### 其他优化
- 自动备份：每次启动自动备份上一版本到 `versions/` 目录
- 版本号更新至 v2.1-performance

### 📚 词库大规模扩充（全量扩充）

#### 广告法违禁词库
- **从 103 条扩充到 509 条**（超过 500+ 目标）
- 新增分类：
  - 极限词扩展（最X/极X/首X/独X/唯X/顶X 系列）
  - 绝对化用语扩展（国家级/最高级/第一等）
  - 虚假承诺扩展（医疗/金融/效果类）
  - 误导性表述扩展（价格类）
  - 贬低对比类、数据无依据类、未审批声称类
  - 医疗保健限制类、引诱消费类、低俗暗示类
  - 涉政敏感类、教育/培训限制、房地产广告限制
  - 烟酒广告限制、收藏品/珠宝广告限制
- 生成脚本：`generate_ad_law.py` / `generate_ad_law2.py` / `generate_ad_law3.py` / `generate_ad_law4.py`

#### 平台规则词库
- **从 36 条扩充到 232 条**（超过 200+ 目标）
- **小红书**：从 16 条扩充到 **110 条**
  - 导流违规扩展（微信/公众号/淘宝/外部链接）
  - 虚假种草扩展（ Pseudo-original / 假测评）
  - 营销限制扩展（硬广/带货/私信推广）
  - 内容违规扩展（低俗/暴力/政治/医美）
  - 医疗健康扩展（减肥/祛斑/祛痘）
  - 金融违规扩展（理财/股票推荐）
  - 未成年人保护（诱导打赏/未成年人出镜）
- **抖音**：从 12 条扩充到 **62 条**
  - 社区规范扩展（低俗/暴力/政治/医美）
  - 营销违规扩展（硬广/导流/虚假宣传）
  - 未成年人保护、金融违规、医疗健康
- **微信视频号**：从 8 条扩充到 **60 条**
  - 内容安全扩展（政治/暴力/低俗/谣言）
  - 营销违规扩展（诱导分享/关注/虚假宣传）
  - 医疗健康、金融投资、未成年人保护
- 生成脚本：`generate_platform_rules.py` / `generate_platform_rules2.py`

#### 蓝V限制词库
- **从 16 条扩充到 57 条**（超过 50+ 目标）
- 新增分类：
  - 医疗声明扩展（治愈率/有效率/安全无副作用）
  - 金融承诺扩展（保本/无风险/最高收益）
  - 教育承诺扩展（保过/必中/100%就业）
  - 法律服务扩展（包赢/必胜/关系运作）
  - 医疗器械、药品、保健品、特殊食品
- 生成脚本：`generate_bluev_regex.py`

#### 正则模式匹配
- **从 5 个扩充到 13 个**（新增 8 个）
- 新增正则模式：
  - "首X" 系列：`首[创发家款次个批]`
  - "独X" 系列：`独[家创有特无]`
  - "唯X" 系列：`唯[一独家]`
  - "顶X" 系列：`顶[级尖配流端]`
  - 数字+第一 / 第X（数字）：`\d+第一|第\d+`
  - 100%效果类：`100%|百分之百|百分百`
  - 永远/永久类：`永[久远]`
- 覆盖更多词根组合，减少关键词枚举

### 🐛 Bug 修复
- **修复 checker.py 重复初始化**：第 38-39 行 `_init_text_tags()` 被调用两次，删除重复调用
- **修复 detector.py 缺失方法**：添加 `_save_rules()` 方法，解决词库管理页面删除广告法/蓝V限制词条时报错的问题

### 📊 数据统计
- **总规则数：811 条**（509 广告法 + 232 平台规则 + 57 蓝V限制 + 13 正则模式）
- 支持平台：小红书 / 抖音 / 微信视频号
- AI 模型：qwen2.5:7b (4.7GB, 本地运行)
- 检测速度：<100ms（500+ 关键词，Aho-Corasick 算法）

### 🔖 版本备份
- 创建 `versions/v2.1-performance/` 备份目录
- 备份文件：`ui/` / `core/` / `rules/` / `app.py` / `run.sh` / `run.bat`

---

## v2.0 — 检测引擎全面升级 (2026-07-06)

### 🚀 重大升级

#### 检测引擎 v2.0
- **词库扩充**：广告法违禁词从 49 条扩充到 103 条
  - 新增"极"系列：极为、极品、极大、极高、极低、极佳、极度
  - 新增"首"系列：首个、首次、首款、首创、首发、首家
  - 新增"独"系列：独家、独创、独有、独一无二
  - 新增其他：无敌、无双、无与伦比、永久、终身、万能、神器、绝版、顶配、顶流等
  - 新增虚假承诺：无副作用、安全无毒、立竿见影、见效快、立即见效
  - 新增误导性表述：全网首发、内部价、成本价、底价、仅此一次、最后机会
- **正则模式匹配**：新增 5 组正则模式，自动捕获"最X""极X""第X""X级"等词根组合
- **检测层级**：关键词精确匹配 + 正则模式补充，双层覆盖

#### AI 语义增强检测
- 集成 **Ollama qwen2.5:7b** 本地大模型
- AI 深度检测分析：隐性违规、虚假宣传、平台风险、蓝V限制
- 异步线程化执行，不阻塞 UI
- 完整 JSON 解析（ANSI 转义清理 + 换行兼容 + 括号匹配提取）

#### 性能优化
- **检测线程化**：检测逻辑在后台线程执行，UI 不再卡顿
- **索引转换优化**：预计算行偏移表 + 二分查找，O(log N) 复杂度
- **字数统计 debounce**：300ms 防抖，减少频繁触发
- **LLM 超时处理**：120 秒超时 + 友好错误提示

#### UI 优化
- 检测按钮区精简布局
- 新增「AI深度检测」按钮
- 违规详情标注匹配类型（关键词/正则）
- LLM 分析结果独立卡片展示（蓝色主题）
- 设置页面新增 Ollama 状态检测
- 版本号更新至 v2.0

### 📊 数据统计
- 总规则数：155 条（103 广告法 + 36 平台规则 + 16 蓝V限制 + 5 正则模式）
- 支持平台：小红书 / 抖音 / 微信视频号
- AI 模型：qwen2.5:7b (4.7GB, 本地运行)

---

## v1.0 — MVP 初始版本 (2026-07-05)

### 首次发布
- CustomTkinter 桌面应用，Apple 风格 UI
- 4 大词库：广告法违禁词 + 平台规则 + 蓝V限制 + 用户自定义
- 全文高亮预览（红/黄/蓝三色标注）
- 词库管理（增删改 + 导入 + 热重载）
- 仪表盘 + 统计卡片
- 跨平台支持（Mac .command + Windows .bat）
- 版本备份系统
